/*
   Test includes:
       * help
       * arg usage
       * subcommand
       * value enum
       * validation
       * arg group
       * hook
       * derive+build mix

* */
fn main() {
    println!("Hello, world!");
}
